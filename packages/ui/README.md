# @clinara/ui

Accessibility-first React component library shared by `apps/web`. Scaffold placeholder;
components (inbox item, reason-code panel, trend chart, approval controls) arrive in
Phase 1 with WCAG-compliant patterns (spec §6.10, §6.11).
