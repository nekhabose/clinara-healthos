"""Integrations persistence models.

Scaffold placeholder — implemented in a later phase. All models must
inherit from `core.models.TenantScopedModel` so PostgreSQL RLS applies.
"""

# from core.models import TenantScopedModel
