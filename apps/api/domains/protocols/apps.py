from django.apps import AppConfig


class ProtocolsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "domains.protocols"
    label = "protocols"
    verbose_name = "Protocols"
