# integration-gateway

**Status:** Boundary within the modular monolith (Phase 0).

Per spec §7.3, this is a future service-extraction target. It currently lives
as a bounded module inside `apps/api`; it will be split out only when scaling,
isolation, ownership, or reliability requirements justify it.
