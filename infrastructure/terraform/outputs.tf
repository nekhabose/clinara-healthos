# Outputs surfaced to CI/CD and app config. Populated as modules are implemented.

# output "database_endpoint" { value = module.database.endpoint }
# output "redis_endpoint"    { value = module.cache.endpoint }
# output "alb_dns_name"      { value = module.compute.alb_dns_name }
